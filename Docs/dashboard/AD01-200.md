# Antigravity Dashboard Change Logs (AD01 - AD200)

This document tracks changes made to the `dashboard` project. Entries are sorted by latest at the top. After the AD200 log, make a new file (AD201-400.md).

### [AD96] TikTok Pixel & Events API Settings Menu, Dedicated Page, and Full Functionality Integration

- **Description**:
  1. **Sidebar Navigation (`dashboard/src/components/dashboard/app-sidebar.jsx`)**:
     - Added dedicated `TikTok Pixel` submenu item under `Settings` directly beneath `Meta Pixel`.
     - Integrated authentic TikTok musical note brand SVG icon (`TikTokIcon`) with responsive sizing and active route highlighting.
  2. **Dashboard Router (`dashboard/src/router.jsx`)**:
     - Registered `/dashboard/settings/tiktok-pixel` route protected by `RoleGuard menuKey="settings"`.
     - Cleaned unused component imports (`SettingsPage`, `AssetsManager`) to maintain high linting hygiene.
  3. **TikTok Pixel & Events API Page (`dashboard/src/pages/dashboard/settings/tiktokPixel.jsx`)**:
     - Created full-featured administration page supporting Pixel ID, masked Events API Access Token (with visibility toggle), and optional Test Event Code.
     - Provided toggle controls for Master Tracking, Browser-Side Pixel (`ttq.js`), TikTok Events API (server-side), and Advanced Customer Matching (SHA-256 phone/email hashing).
     - Built live "Test Events API Connection" action verifying credentials against TikTok API with instant status feedback (Connected & Verified, Connection Failed, Untested badges).
     - Incorporated interactive setup guide cards covering TikTok Ads Manager credential retrieval, token generation, and `purchase_{orderNumber}` event deduplication.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx` (MODIFIED)
  - `dashboard/src/router.jsx` (MODIFIED)
  - `dashboard/src/pages/dashboard/settings/tiktokPixel.jsx` (NEW)
  - `Docs/dashboard/AD01-200.md` (NEW)
- **Impact**: Enables store operators to configure, verify, and monitor TikTok tracking and server-side Events API conversions from the dashboard alongside Meta Pixel.

---
